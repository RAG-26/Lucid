{
  "name": "@lucid/eval",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "eval": "tsx src/cli.ts",
    "build": "tsc -p tsconfig.build.json",
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@lucid/core": "*",
    "commander": "15.0.0",
    "dotenv": "17.4.2"
  }
}
