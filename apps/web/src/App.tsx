// The real chat UI (citations, abstention UX, pipeline trace inspector) lands here once
// apps/api's /api/chat route exists — see CLAUDE.md's repo layout.
export function App() {
  return <h1>Lucid</h1>;
}
